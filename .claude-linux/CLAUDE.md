# CLAUDE.md

## 基本方針

- シンプルな構成・処理を優先する。
- コメント、コミットメッセージなどには、日本語を使用する。

## コミット

- 変更単位は小さく、こまめにコミットする。
- 変更を反映するたびにコミットしてよい（指示不要）。

## エージェント活用方針

- 独立タスク（調査・レビュー・テスト等）は複数サブエージェントで並列処理を優先する。
- 文脈窓の節約や待ち時間削減のためのサブエージェント活用も積極的に行う。
